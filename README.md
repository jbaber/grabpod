grabpod.py is an elementary CLI podcast fetcher.

example_grabpodrc.yaml should be a sufficient example to start downloading.

INSTALL PREREQUISITES
---------------------

    pip install --requirement REQUIRES
